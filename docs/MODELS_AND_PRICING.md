# AIモデルと料金体系

**最終更新**: 2026-04-14
**バージョン**: v2.1.0
**ステータス**: 編集モデルを gemini-2.5-flash-image へ移行（コスト71%削減）

---

## 🏗️ 現在のモデル構成（3層アーキテクチャ）

| レイヤー | モデル | 対象エンドポイント | 単価目安 |
|---|---|---|---|
| **画像生成（4K）** | `gemini-3.1-flash-image-preview` | generate-poster | 約¥10/枚 |
| **画像編集・Inpainting** | `gemini-2.5-flash-image` | unified-edit, edit, edit-region, insert | 約¥6/回 |
| **高度解析** | `gemini-3.1-pro-preview` | analyze-image, extract-text-layers, extract-blueprint | 約¥0.17/回 |
| **軽量判定** | `gemini-3.1-flash-lite-preview` | smart-edit（テキスト判定） | 約¥0.005/回 |

> [!NOTE]
> **2026-04-14 更新**: 編集モデルを `gemini-3-pro-image-preview` → `gemini-2.5-flash-image` に移行。
> Inpainting対応・精密な局所編集・オブジェクト完全削除に強化。コストは約¥20→¥6（71%削減）。
> **Update**: オブジェクト消去（remove/消して等）時は、専用の強力なプロンプトとフェザリング付きマスク画像を動的に生成し、高精度な背景再構築を実行します。

---

## 🚀 コスト最適化の成果

### ワークフロー別コスト比較（1枚生成＋3回テキスト編集）

| 操作 | 移行前（2025年末） | 移行後（現在） | 削減率 |
|---|---|---|---|
| **新規生成（4K）** | ¥20（旧3.0 Pro） | **¥10**（3.1 Flash 4K） | 約50%減 |
| **テキスト編集1回目** | ¥20（画像再生成） | **¥0.005**（Flash-Lite JSON更新） | **約99.97%減 🔥** |
| **テキスト編集2回目** | ¥20（画像再生成） | **¥0.005**（同上） | 約99.97%減 |
| **テキスト編集3回目** | ¥20（画像再生成） | **¥0.005**（同上） | 約99.97%減 |
| **合計** | **¥80** | **約¥10.015** | **約87.5%削減** |

### smart-edit の Flash-Lite 化による削減（3月9日適用）

smart-editは「テキスト変更か？画像変更か？」を判定するだけのため、最安クラスのモデルで十分：

| 変更前 | 変更後 | 1回あたりコスト削減 |
|---|---|---|
| `gemini-3.1-pro-preview` | `gemini-3.1-flash-lite-preview` | 約**97%削減**（¥0.2 → ¥0.005） |

月100回の編集で **¥20 → 約¥0.5/月** に。

### 編集モデルの 2.5 Flash Image 移行による削減（4月14日適用）

| 変更前 | 変更後 | 1回あたりコスト削減 |
|---|---|---|
| `gemini-3-pro-image-preview`（¥20/回） | `gemini-2.5-flash-image`（¥6/回） | 約**71%削減** |

画像編集（Inpainting・オブジェクト削除・矩形領域編集）で月50回使用した場合: **¥1,000 → ¥300/月**

---

## 📊 月額コスト試算

### パターン1: 新規生成のみ

| 月間枚数 | 解析コスト | 生成コスト（4K） | 合計 |
|---|---|---|---|
| 50枚 | ¥9 | ¥500 | **¥509** |
| 100枚 | ¥17 | ¥1,000 | **¥1,017** |
| 300枚 | ¥51 | ¥3,000 | **¥3,051** |

### パターン2: 1枚につき平均3回のテキスト編集あり

| 生成枚数 | Smart Edit回数 | 生成コスト | 編集コスト | 合計 |
|---|---|---|---|---|
| 100枚 | 300回 | ¥1,017 | **約¥1.5** | **¥1,018** |
| 300枚 | 900回 | ¥3,051 | **約¥4.5** | **¥3,055** |

> 旧アーキテクチャでは100枚＋300回編集で **¥8,000+** → 現在 **¥1,018**（**約87%削減**）

---

## 🎯 各最適化の詳細

### 1. 4K高解像度化（2026-03-09 適用）
`imageSize: '2K'` → `'4K'` に変更。コストは同じ約¥10のまま、出力品質が向上。

### 2. キャラクターSeed永続化（2026-03-09 適用）
`character_features.seed` をレスポンスに含め、フロントエンドが次回生成時に渡すことで同一キャラクターが再現される。コスト影響なし。

### 3. smart-editへのthinkingConfig追加（2026-03-09 適用）
`thinkingBudget: 1024`（medium相当）を設定。分類精度向上でFalse positiveによる不要な画像生成呼び出しを削減。

### 4. Context Caching（in-memoryキャッシュ）
extract-blueprintの解析結果をTTL10分でキャッシュ。同一セッション内の連続解析コストをほぼゼロに。

---

## 🔧 環境変数による切り替え

```env
# 生成モデル（デフォルト: gemini-3.1-flash-image-preview）
GEMINI_IMAGE_MODEL=gemini-3.1-flash-image-preview

# 編集モデル（2026-04-14 移行: gemini-2.5-flash-image）
GEMINI_EDIT_MODEL=gemini-2.5-flash-image

# 解析モデル（デフォルト: gemini-3.1-pro-preview）
GEMINI_ANALYSIS_MODEL=gemini-3.1-pro-preview

# Smart Editモデル（デフォルト: gemini-3.1-flash-lite-preview）
GEMINI_SMART_EDIT_MODEL=gemini-3.1-flash-lite-preview
```

再デプロイ不要で即時切り替え可能。ロールバックする場合は `GEMINI_EDIT_MODEL=gemini-3-pro-image-preview` に戻す。

---

## 💡 料金節約のヒント

1. **テキスト修正はSmart Edit経由**（自動）— 画像再生成が発生しないため¥0.005/回
2. **矩形編集は最小範囲に限定** — 1回約¥4なので多用注意
3. **同一セッションで連続作業** — Contextキャッシュが効いて解析コストゼロ
4. **レート制限の活用** — デフォルト100回/日。管理者画面から個別調整可能

---

**更新者**: Antigravity AI
**更新日**: 2026-03-09
